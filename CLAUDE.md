# CLAUDE.md

## Project Overview

**info30** is a newly initialized repository. It does not yet contain application code, build tooling, or test infrastructure. This file should be updated as the project evolves.

## Repository Structure

```
info30/
├── CLAUDE.md       # AI assistant guidance (this file)
└── README.md       # Project description
```

## Development Setup

No build system, package manager, or runtime dependencies are configured yet. When they are added, document the setup steps here.

## Testing

No test framework is configured. When tests are added, document how to run them here (e.g., `npm test`, `pytest`, `make test`).

## Linting & Formatting

No linting or formatting tools are configured. When added, document the commands here.

## Git Workflow

- **Primary branch:** `master`
- Feature branches should use descriptive names.
- Write clear, concise commit messages summarizing the "why" of each change.

## Conventions

As the codebase grows, document the following here:

- Programming language(s) and version(s)
- Code style and naming conventions
- Directory layout conventions
- Error handling patterns
- Dependency management approach

## Notes for AI Assistants

- Read existing code before proposing changes.
- Keep changes minimal and focused on the task at hand.
- Do not add unnecessary abstractions, comments, or features beyond what is requested.
- Update this file when significant tooling, structure, or workflow changes are introduced.
